<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RedFoxGame" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorPrimary">@color/primary_green</item>
        <item name="android:colorAccent">@color/accent_gold</item>
        <item name="android:textColorPrimary">@color/on_background</item>
        <item name="android:textColorSecondary">@color/on_background_secondary</item>
    </style>

    <!-- Тема для splash-экрана -->
    <style name="Theme.RedFoxGame.Splash" parent="Theme.RedFoxGame">
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>
