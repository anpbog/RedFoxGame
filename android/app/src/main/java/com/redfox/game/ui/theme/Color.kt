package com.redfox.game.ui.theme

import androidx.compose.ui.graphics.Color

// Основные цвета бренда
val RedFoxOrange = Color(0xFFFF6B35)

// Тёмная тема — фоны (синхронизировано с colors.xml)
val DarkBackground = Color(0xFF1A1A2E)
val DarkSurface = Color(0xFF16213E)
val DarkSurfaceVariant = Color(0xFF0F3460)
val DarkCard = Color(0xFF1C2128)

// Текст
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB0BEC5)
val TextTertiary = Color(0xFF6E7681)

// Игровые цвета
val PoolUp = Color(0xFF00C853)          // Зелёный — рост (UP)
val PoolUpLight = Color(0xFF3FB950)
val PoolDown = Color(0xFFFF1744)        // Красный — падение (DOWN)
val PoolDownLight = Color(0xFFFF6B6B)

// График
val ChartLine = Color(0xFFFFD700)       // Линия цены BTC (золотая)
val ChartGrid = Color(0xFF21262D)

// Утилитарные
val SuccessGreen = Color(0xFF00C853)
val WarningYellow = Color(0xFFD29922)
val ErrorRed = Color(0xFFFF1744)
val InfoBlue = Color(0xFF388BFD)
val AccentGold = Color(0xFFFFD700)
val Disabled = Color(0xFF37474F)

// Полупрозрачные (для градиентов графика)
val UpZoneTransparent = Color(0x4D00C853)
val DownZoneTransparent = Color(0x4DFF1744)
